name: .NET

on:
  pull_request:
    branches: [ main, development ]
  push:
    branches: [ main, development, shopify-monitor ]

jobs:
  build-test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore KachingWindows.sln

      - name: Build
        run: dotnet build KachingWindows.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test KachingWindows.sln --configuration Release --no-build --verbosity normal
